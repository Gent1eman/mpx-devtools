# Mpx 源码级跨端调试器 TODO

> 本清单按依赖顺序排列。  
> 执行规则：每次开发只选择一个未完成任务；完成验收、测试和文档后，才能开始下一项。

## 1. 单任务执行规则

每次迭代必须遵循：

1. 只领取一个 TODO ID。
2. 开始前明确该任务的输入、输出和验收条件。
3. 不顺便实现相邻功能。
4. 新发现的问题记录成新的 TODO，不扩大当前任务范围。
5. 当前任务必须有自动化测试或可重复的人工验证步骤。
6. 当前任务完成后更新复选框，并记录实现说明。
7. 验收失败时只修复当前任务，不开始下一任务。

状态定义：

```text
[ ] 未开始
[-] 进行中
[x] 已完成并验收
[!] 被阻塞，需要记录阻塞原因
```

## 2. 阶段 0：项目基础

### T0001：创建 Monorepo 骨架

- [x] 创建 pnpm workspace 和 `packages/` 目录。
- 交付物：根 `package.json`、`pnpm-workspace.yaml`、基础目录。
- 验收：执行 `pnpm install` 成功，workspace 能识别所有空包。
- 实现说明（2026-08-14）：建立 pnpm workspace，并为设计中的九个发布包创建最小 package manifest。

### T0002：建立统一 TypeScript 配置

- [x] 增加根 `tsconfig` 和包级继承配置。
- 交付物：TypeScript 配置文件。
- 验收：一个最小 TypeScript 文件可在两个 workspace 包中通过类型检查。
- 依赖：T0001。
- 实现说明（2026-08-14）：根项目使用 TypeScript project references；全部包继承根基础配置。`protocol` 与 `runtime` 包的最小源码已通过根类型检查。

### T0003：建立统一测试命令

- [x] 接入 Vitest 并提供根测试命令。
- 交付物：Vitest 配置和一个示例测试。
- 验收：`pnpm test` 执行成功且包含一个通过的测试。
- 依赖：T0002。
- 实现说明（2026-08-14）：根项目使用 Vitest，测试范围为 workspace 包内的 `*.test.ts` 文件；`protocol` 包包含可重复运行的示例测试。

### T0004：建立代码质量检查

- [x] 配置 ESLint 和格式检查。
- 交付物：Lint 配置和根命令。
- 验收：`pnpm lint` 对所有包执行成功。
- 依赖：T0002。
- 实现说明（2026-08-15）：使用 ESLint flat config 检查 JavaScript 和 TypeScript；Prettier 负责项目格式检查，并提供写入格式化命令。

### T0005：建立最小 Mpx 微信示例项目

- [x] 创建只包含一个页面和一个按钮的 Mpx 示例。
- 交付物：`examples/wx-minimal`。
- 验收：原始 Mpx 命令可以生成微信小程序产物。
- 实现说明（2026-08-15）：新增独立 workspace 示例，包含单页计数按钮和微信开发者工具配置；使用 Mpx CLI 的 `build:wx` 命令成功生成微信小程序产物。

### T0006：记录基线构建性能

- [x] 记录示例项目首次编译和增量编译耗时。
- 交付物：`docs/performance-baseline.md`。
- 验收：文档包含环境、命令、首次编译和增量编译数据。
- 依赖：T0005。
- 实现说明（2026-08-15）：记录 wx-minimal 示例的冷构建与单文件变更后的增量构建耗时，并给出可重复的测量命令。

## 3. 阶段 1：调试协议

### T0101：定义协议版本模型

- [x] 在 `protocol` 包中定义 `protocolVersion`。
- 验收：版本类型、常量和单元测试存在。
- 依赖：T0002。
- 实现说明（2026-08-15）：定义当前协议版本常量、受支持版本类型、通用版本字段和版本校验函数，并覆盖支持与拒绝版本的单元测试。

### T0102：定义会话握手类型

- [x] 定义 `session.hello` 消息类型。
- 验收：合法消息通过类型和 Schema 校验，缺少 `buildId` 时失败。
- 依赖：T0101。
- 实现说明（2026-08-15）：定义 `session.hello` 的严格 Zod Schema，包含协议版本、非空构建 ID 与目标平台；覆盖合法消息、缺失构建 ID 和空构建 ID 的测试。

### T0103：定义通用调试事件类型

- [x] 定义 `DebugEvent` 基础结构。
- 验收：事件必须包含 `eventId`、`sessionId`、`buildId`、目标和时间戳。
- 依赖：T0101。
- 实现说明（2026-08-15）：定义严格的 DebugEvent 基础 Schema，包含版本、事件/会话/构建身份、目标、时间戳、优先级与事件类别，并验证所有必填身份字段。

### T0104：定义页面事件类型

- [x] 定义页面创建、显示、隐藏和卸载事件。
- 验收：四种页面事件均有 Schema 测试。
- 依赖：T0103。
- 实现说明（2026-08-15）：定义 `page.create`、`page.show`、`page.hide`、`page.unload` 的页面事件 Schema，要求页面实例 ID 和路由，并覆盖四种事件的校验测试。

### T0105：定义组件事件类型

- [x] 定义组件创建和销毁事件。
- 验收：组件事件可以关联页面实例和父组件。
- 依赖：T0103。
- 实现说明（2026-08-15）：定义 `component.create` 与 `component.destroy` Schema，要求页面与组件实例 ID，并支持父组件实例 ID 关联；覆盖嵌套组件和页面根组件测试。

### T0106：定义方法探针事件类型

- [x] 定义方法命中事件。
- 验收：事件可以携带 `probeId` 和可选快照 ID。
- 依赖：T0103。
- 实现说明（2026-08-15）：定义 `method` 探针命中事件 Schema，要求非空 `probeId`，支持可选 `snapshotId`，并覆盖有无快照与缺失探针 ID 的测试。

### T0107：定义变量快照类型

- [x] 定义参数、局部变量和组件状态快照结构。
- 验收：快照可以区分参数、局部变量、状态、返回值和错误。
- 依赖：T0103。
- 实现说明（2026-08-15）：定义带快照身份、采集时间和探针身份的严格快照 Schema；支持参数、局部变量、组件状态、返回值和错误字段，并覆盖五类快照测试。

### T0108：定义探针控制命令

- [x] 定义探针开启和关闭命令。
- 验收：命令必须包含 `probeId`、启用状态和采集选项。
- 依赖：T0106。
- 实现说明（2026-08-15）：定义严格的 `probe.update` 控制命令，包含请求 ID、探针 ID、启用状态和参数/组件状态/调用栈采集选项，并覆盖必填字段校验。

## 4. 阶段 2：本地调试服务最小链路

### T0201：启动 Fastify 服务

- [x] 实现可配置端口的本地 HTTP 服务。
- 验收：`GET /health` 返回版本和运行状态。
- 依赖：T0002。
- 实现说明（2026-08-15）：新增默认仅监听 `127.0.0.1:4399` 的 Fastify 服务工厂与启动函数；`GET /health` 返回服务版本和运行状态，端口与主机均可配置。

### T0202：提供静态首页

- [x] 由 Server 返回一个最小调试首页。
- 验收：浏览器访问 Server 根地址可显示页面。
- 依赖：T0201。
- 实现说明（2026-08-15）：Server 根路径返回最小 HTML 调试首页，显示服务运行状态并链接健康检查端点。

### T0203：启动 WebSocket Gateway

- [x] 在 Server 上提供 WebSocket 连接。
- 验收：测试客户端连接成功并收到欢迎消息。
- 依赖：T0201。
- 实现说明（2026-08-15）：新增 `/ws` WebSocket Gateway，连接成功后发送 `server.welcome`；使用真实 WebSocket 客户端完成连接与消息测试。Server 包会随根类型检查编译为可启动的本地 JavaScript 模块。

### T0204：实现会话握手校验

- [x] 校验 `session.hello`。
- 验收：合法握手建立会话，非法协议版本被拒绝。
- 依赖：T0102、T0203。
- 实现说明（2026-08-15）：WebSocket 客户端在欢迎消息后发送 `session.hello`；合法握手收到 `session.accepted`，无效协议版本收到拒绝响应并关闭连接。

### T0205：实现随机会话 Token

- [x] Server 启动时生成随机 Token。
- 验收：错误 Token 无法建立会话。
- 依赖：T0204。
- 实现说明（2026-08-15）：Server 创建时生成 256 位随机十六进制 Token（支持 `options.token` 注入），握手时校验 `session.hello` 中的 `token` 字段；协议 Schema 新增必填 `token`。错误 Token 返回 `session.rejected`（reason `invalid-session-token`）并关闭连接，每次启动生成的 Token 互不相同。

### T0206：实现单会话内存管理

- [x] 保存一个运行时会话的连接状态。
- 验收：可以查询连接、断开和最后活动时间。
- 依赖：T0204。
- 实现说明（2026-08-15）：新增 `SessionManager` 单会话状态模块；握手成功后生成 `sessionId` 并在 `session.accepted` 中返回，记录 `buildId`/`target`/`connected`/`connectedAt`/`lastActivityAt`；新增 `GET /api/session` 查询连接状态，socket 关闭时标记断开，收到消息时刷新最后活动时间。

### T0207：接收单条调试事件

- [x] 接收并校验一条 `DebugEvent`。
- 验收：合法事件进入内存，非法事件返回协议错误。
- 依赖：T0103、T0206。
- 实现说明（2026-08-15）：新增 `EventStore` 内存事件存储；握手成功后，后续 WebSocket 消息按 `DebugEventSchema` 校验，合法事件存入内存并刷新最后活动时间，非法事件返回 `event.rejected`（reason `invalid-event`）且不关闭连接。

### T0208：实现事件环形缓冲区

- [x] 将事件保存在固定容量的内存缓冲区。
- 验收：容量为 N 时写入 N+1 条事件会覆盖最旧事件。
- 依赖：T0207。
- 实现说明（2026-08-15）：`EventStore` 改为固定容量环形缓冲（head/size 双指针，O(1) 追加），默认容量 `DEFAULT_EVENT_BUFFER_CAPACITY = 10_000`；容量写满后每追加一条覆盖最旧一条，`list()` 按插入顺序（最旧在前）返回副本；非正整数或非整数容量抛出 `RangeError`。

### T0209：提供事件查询 API

- [x] 实现 `GET /api/events`。
- 验收：按时间顺序返回缓冲区事件。
- 依赖：T0208。
- 实现说明（2026-08-15）：新增 `GET /api/events`，返回环形缓冲区内的事件数组（最旧在前、按到达顺序），空缓冲返回 `[]`。

### T0210：将新事件推送给 UI

- [x] Server 通过 WebSocket 将新事件推送给 UI 客户端。
- 验收：测试 UI 客户端能实时收到一条事件。
- 依赖：T0208。
- 实现说明（2026-08-15）：新增 `/ws/ui` WebSocket 端点供 UI 客户端连接；Runtime 事件校验入库后，Server 以 `{ type: 'event', event }` 广播给所有已连接的 UI 客户端（仅对 open 状态连接发送），客户端断开时自动移除。

## 5. 阶段 3：微信 Runtime 基础

### T0301：建立 Runtime 核心接口

- [ ] 实现 `initialize`、`emit` 和 `dispose` 空实现。
- 验收：Runtime 能初始化并安全重复销毁。
- 依赖：T0103。

### T0302：实现微信 WebSocket Transport

- [ ] 使用 `wx.connectSocket` 连接 Server。
- 验收：微信开发者工具中的示例项目可以完成握手。
- 依赖：T0204、T0301。

### T0303：实现 Runtime 事件队列

- [ ] 在 Runtime 中缓存待发送事件。
- 验收：Transport 未连接时业务调用不抛错，事件进入队列。
- 依赖：T0302。

### T0304：实现批量发送

- [ ] 按数量或时间间隔批量发送事件。
- 验收：连续产生 100 条事件时发送批次数小于事件数。
- 依赖：T0303。

### T0305：实现断线重连

- [ ] WebSocket 断开后按退避策略重连。
- 验收：重启 Server 后 Runtime 能自动恢复连接。
- 依赖：T0302。

### T0306：实现队列容量限制

- [ ] 队列超过容量时丢弃低优先级事件。
- 验收：错误事件在队列溢出后仍被保留。
- 依赖：T0303。

### T0307：实现安全序列化基础能力

- [ ] 支持循环引用、最大深度和最大字符串长度。
- 验收：循环对象序列化不抛错，超限值带截断标记。
- 依赖：T0301。

### T0308：实现默认敏感字段脱敏

- [ ] 对默认敏感字段进行遮蔽。
- 验收：`token` 和 `password` 的原值不会出现在序列化结果中。
- 依赖：T0307。

## 6. 阶段 4：页面识别

### T0401：生成页面实例 ID

- [ ] 页面创建时生成唯一 `pageInstanceId`。
- 验收：同一路由打开两次产生不同 ID。
- 依赖：T0301。

### T0402：采集页面创建事件

- [ ] 页面实例创建时上报 `page.create`。
- 验收：事件包含 route 和实例 ID。
- 依赖：T0104、T0401。

### T0403：采集页面显示事件

- [ ] `onShow` 时上报 `page.show`。
- 验收：Server 能看到当前页面变为 visible。
- 依赖：T0402。

### T0404：采集页面隐藏事件

- [ ] `onHide` 时上报 `page.hide`。
- 验收：Server 能看到页面变为 hidden。
- 依赖：T0403。

### T0405：采集页面卸载事件

- [ ] `onUnload` 时上报 `page.unload`。
- 验收：卸载时间被记录，历史事件仍可查询。
- 依赖：T0404。

### T0406：同步微信页面栈

- [ ] 使用 `getCurrentPages()` 上报页面栈顺序。
- 验收：Server 中的栈顺序与微信开发者工具实际页面一致。
- 依赖：T0403。

### T0407：提供页面查询 API

- [ ] 实现 `GET /api/pages`。
- 验收：返回当前、隐藏和已卸载页面实例。
- 依赖：T0405、T0406。

### T0408：实现页面栈 UI

- [ ] 在浏览器界面显示页面列表和状态。
- 验收：当前页面有明确标识，点击页面可选中它。
- 依赖：T0202、T0407。

### T0409：按页面过滤时间线

- [ ] 选择页面后只显示该页面事件。
- 验收：两个页面产生的事件不会混在选中页面时间线中。
- 依赖：T0408、T0209。

## 7. 阶段 5：编译与 Source Map

### T0501：建立 Webpack 插件空壳

- [ ] 创建能接入 Mpx 项目的 Webpack 插件。
- 验收：插件能收到一次编译开始和完成事件。
- 依赖：T0005。

### T0502：生成 `buildId`

- [ ] 每次成功编译生成构建身份。
- 验收：源码或目标平台变化时 `buildId` 变化。
- 依赖：T0501。

### T0503：输出构建清单

- [ ] 生成 `.mpx-debug/manifest.json`。
- 验收：清单包含目标平台、构建 ID 和源文件输出关系。
- 依赖：T0502。

### T0504：启用 `hidden-source-map`

- [ ] 调试模式下生成外部 Source Map。
- 验收：产物存在 `.map`，业务 JS 不包含 `sourceMappingURL`。
- 依赖：T0501。

### T0505：建立 Source Map 索引

- [ ] 将生成文件映射到对应 Source Map 文件。
- 验收：可通过生成文件名查询 map 路径。
- 依赖：T0503、T0504。

### T0506：实现单帧位置映射

- [ ] 将生成代码行列还原到原始源码。
- 验收：固定 fixture 的一个位置准确映射到 `.mpx` 行列。
- 依赖：T0505。

### T0507：解析 JavaScript 错误堆栈

- [ ] 将错误字符串解析为 Stack Frame。
- 验收：微信格式错误栈可以解析函数、文件、行和列。
- 依赖：T0506。

### T0508：还原完整错误堆栈

- [ ] 对错误的每个 Frame 执行源码映射。
- 验收：至少一个三层调用栈准确还原到业务源码。
- 依赖：T0507。

### T0509：校验运行时 `buildId`

- [ ] Server 拒绝映射不匹配构建的事件。
- 验收：旧 Runtime 连接时 UI 显示构建不一致。
- 依赖：T0502、T0204。

### T0510：建立 Source Map 黄金测试集

- [ ] 增加基础方法、async、TypeScript 和条件编译 fixture。
- 验收：所有 fixture 映射结果固定并通过自动化测试。
- 依赖：T0508。

## 8. 阶段 6：方法探针与参数

### T0601：建立 Babel 插件空壳

- [ ] 创建不改变代码语义的 Babel 插件。
- 验收：输入输出行为一致且 Source Map 存在。
- 依赖：T0002。

### T0602：识别 Mpx methods 方法

- [ ] AST 中识别 `methods` 对象的方法。
- 验收：fixture 中的方法名和源码位置识别正确。
- 依赖：T0601。

### T0603：生成稳定方法 Probe ID

- [ ] 根据路径、语义和 AST 指纹生成 ID。
- 验收：只修改空白和注释时 ID 不变。
- 依赖：T0602。

### T0604：插入方法入口探针

- [ ] 在单个 methods 方法入口插入 `hit`。
- 验收：原方法返回值、`this` 和异常行为不变。
- 依赖：T0603。

### T0605：将 Babel 插件接入 Mpx 编译

- [ ] 调试构建自动执行入口插桩。
- 验收：示例项目产物包含一个探针调用。
- 依赖：T0501、T0604。

### T0606：注入 Debug Runtime

- [ ] 调试产物自动包含 Runtime，全局探针可调用。
- 验收：业务源码无需手工 import Runtime。
- 依赖：T0301、T0605。

### T0607：上报方法命中事件

- [ ] 探针命中后发送 `method` 事件。
- 验收：点击示例按钮后 Server 收到对应 Probe ID。
- 依赖：T0606、T0207。

### T0608：采集方法参数快照

- [ ] 开启采集时序列化方法参数。
- 验收：UI 能看到示例方法的参数名或索引和值。
- 依赖：T0107、T0307、T0607。

### T0609：采集 `this` 摘要

- [ ] 开启采集时保存组件实例状态摘要。
- 验收：快照包含组件 ID 和受限深度的状态。
- 依赖：T0608。

### T0610：采集调用栈

- [ ] 开启采集时保存 JavaScript 调用栈。
- 验收：调用栈可以通过 Source Map 还原到 `.mpx`。
- 依赖：T0508、T0607。

### T0611：实现方法级动态开关

- [ ] Server 可以开启或关闭指定 Probe ID。
- 验收：关闭时不产生快照，开启时产生快照，不重新编译。
- 依赖：T0108、T0608。

## 9. 阶段 7：组件树

### T0701：生成组件实例 ID

- [ ] Mpx 组件创建时生成唯一实例 ID。
- 验收：同类组件的多个实例拥有不同 ID。
- 依赖：T0301。

### T0702：采集组件创建事件

- [ ] 上报组件名称、源码 ID 和页面实例 ID。
- 验收：Server 能关联页面和组件。
- 依赖：T0105、T0701。

### T0703：记录父子组件关系

- [ ] 组件创建时记录父组件 ID。
- 验收：Server 能构建至少三层组件树。
- 依赖：T0702。

### T0704：采集组件销毁事件

- [ ] 组件销毁时上报时间。
- 验收：销毁组件不再显示为活动实例。
- 依赖：T0702。

### T0705：提供组件树 API

- [ ] 实现 `GET /api/components`。
- 验收：按页面实例返回树形结构。
- 依赖：T0703、T0704。

### T0706：实现组件树 UI

- [ ] 在选中页面中展示组件树。
- 验收：点击组件后能获得对应组件实例 ID。
- 依赖：T0408、T0705。

### T0707：按组件过滤时间线

- [ ] 选择组件后只显示该组件相关事件。
- 验收：父子组件事件可以独立过滤。
- 依赖：T0706、T0209。

### T0708：记录组件源码路径与运行时元信息

- [ ] 在组件创建事件中记录相对源码路径、组件类型和创建时间。
- 验收：同名组件的多个实例均可定位到各自的 `.mpx` 源码文件，未知来源有明确降级标识。
- 依赖：T0503、T0702。

### T0709：按需采集组件公开状态快照

- [ ] 为选中组件采集 `data`、`props`、`computed` 的安全序列化快照，不保留业务对象引用。
- 验收：示例组件的三类状态能在同一时间点返回；循环引用、敏感字段和超限值符合序列化策略。
- 依赖：T0307、T0308、T0705。

### T0710：实现组件状态检查器 UI

- [ ] 在组件树选择态显示组件元信息和 `data`、`props`、`computed` 三个只读分组。
- 验收：选择不同实例时状态与源码路径同步切换，且不会显示其他页面实例的数据。
- 依赖：T0408、T0706、T0708、T0709。

### T0711：实现当前页面组件状态搜索

- [ ] 支持按关键字搜索当前页面活动组件的 `data`、`props`、`computed`，返回字段路径和值摘要。
- 验收：搜索结果可定位组件实例、过滤时间线并打开对应源码；隐藏或已卸载页面的实例不出现在结果中。
- 依赖：T0407、T0705、T0709、T0710。

## 10. 阶段 8：生命周期与 setData

### T0801：识别生命周期方法

- [ ] AST 中识别首批页面生命周期。
- 验收：`onLoad`、`onShow`、`onHide`、`onUnload` 被正确识别。
- 依赖：T0602。

### T0802：插入生命周期入口探针

- [ ] 为已识别生命周期插入探针。
- 验收：生命周期执行顺序不变。
- 依赖：T0801、T0604。

### T0803：展示生命周期时间线

- [ ] UI 显示生命周期事件。
- 验收：进入和离开页面的顺序与运行时一致。
- 依赖：T0802、T0209。

### T0804：找到 Mpx 统一 `setData` 接入点

- [ ] 完成接入点技术验证并记录结论。
- 交付物：一份只描述接入点的研究记录。
- 验收：明确使用官方钩子、Runtime 包装或编译转换中的一种。

### T0805：采集 `setData` 调用

- [ ] 上报键数量、数据体积和耗时。
- 验收：示例页面一次 `setData` 对应一条事件。
- 依赖：T0804。

### T0806：生成 `setData` 前后差异

- [ ] 计算受影响字段的值变化。
- 验收：UI 能显示 `loading: false → true`。
- 依赖：T0805、T0307。

### T0807：展示 `setData` 时间线

- [ ] UI 将方法事件和 `setData` 事件按时间关联展示。
- 验收：可从 `setData` 事件返回触发方法源码。
- 依赖：T0803、T0806。

## 11. 阶段 9：错误与源码界面

### T0901：采集未捕获 JavaScript 异常

- [ ] Runtime 上报未捕获异常。
- 验收：示例异常能够到达 Server。
- 依赖：T0302、T0507。

### T0902：展示还原后的错误栈

- [ ] UI 展示 `.mpx` 源码调用栈。
- 验收：点击 Stack Frame 可选中源码位置。
- 依赖：T0508、T0901。

### T0903：折叠框架内部调用栈

- [ ] 默认折叠 Mpx 和 Runtime 内部 Frame。
- 验收：用户可以展开查看完整原始栈。
- 依赖：T0902。

### T0904：提供源码读取 API

- [ ] 实现受限的 `GET /api/source`。
- 验收：只能读取项目根目录内的允许文件。
- 依赖：T0201。

### T0905：实现源码查看器

- [ ] UI 使用 Monaco 展示源码并高亮指定行。
- 验收：错误事件可以打开正确文件和行列。
- 依赖：T0902、T0904。

### T0906：提供生成代码读取 API

- [ ] 实现受限的 `GET /api/generated`。
- 验收：只能读取当前构建清单中的产物。
- 依赖：T0503。

### T0907：实现源码与产物双栏

- [ ] 同时显示 `.mpx` 和微信生成 JS。
- 验收：选择 Stack Frame 后两侧对应位置均高亮。
- 依赖：T0905、T0906。

### T0908：实现打开本地源码 API

- [ ] 实现 `POST /api/source/open`。
- 验收：合法项目文件可以打开，路径穿越请求被拒绝。
- 依赖：T0904。

### T0909：在 UI 中打开编辑器

- [ ] 源码位置增加“在编辑器中打开”。
- 验收：点击后编辑器定位到正确行列。
- 依赖：T0908。

## 12. 阶段 10：模板语义映射

### T1001：调研 Mpx 模板 AST 接入点

- [ ] 记录模板解析和代码生成阶段可用钩子。
- 交付物：模板编译接入研究记录。
- 验收：明确是否需要修改 `@mpxjs/webpack-plugin`。

### T1002：定义模板语义映射格式

- [ ] 定义模板节点和事件绑定的 `SemanticEntry`。
- 验收：Schema 可以表达模板源位置和生成位置。
- 依赖：T1001。

### T1003：为模板节点生成稳定 ID

- [ ] 根据模板结构路径生成节点 ID。
- 验收：只修改空白时节点 ID 不变。
- 依赖：T1002。

### T1004：映射一个模板事件

- [ ] 将一个 `@tap` 事件关联到源码模板位置和方法。
- 验收：点击事件后 UI 同时显示模板位置和处理方法位置。
- 依赖：T0607、T1003。

### T1005：建立模板映射黄金测试

- [ ] 增加事件、条件和循环模板 fixture。
- 验收：模板位置映射通过固定结果测试。
- 依赖：T1004。

## 13. 阶段 11：完整时间线 UI

### T1101：实现统一时间线数据模型

- [ ] 将页面、组件、方法和 `setData` 事件映射为统一模型。
- 验收：四种事件能按时间排序。
- 依赖：T0405、T0607、T0704、T0805。

### T1102：实现虚拟滚动时间线

- [ ] UI 流畅显示 10,000 条事件。
- 验收：滚动过程中无明显长时间阻塞。
- 依赖：T1101。

### T1103：实现事件类型过滤

- [ ] 按一种或多种事件类型过滤。
- 验收：过滤不会改变原始事件缓冲区。
- 依赖：T1102。

### T1104：实现事件详情面板

- [ ] 点击事件展示基础字段和源码位置。
- 验收：切换事件时详情同步更新。
- 依赖：T1102、T0905。

### T1105：实现参数面板

- [ ] 在事件详情中展示参数快照。
- 验收：循环引用和截断值有明确标记。
- 依赖：T0608、T1104。

### T1106：实现组件状态面板

- [ ] 展示事件发生时的组件状态。
- 验收：状态快照属于正确组件实例和时间点。
- 依赖：T0609、T1104。

### T1107：实现状态差异面板

- [ ] 展示同一组件连续快照的字段差异。
- 验收：新增、删除和修改字段使用不同标识。
- 依赖：T1106。

### T1108：实现调用栈面板

- [ ] 展示已还原的业务调用栈。
- 验收：点击 Frame 跳转源码。
- 依赖：T0610、T1104。

## 14. 阶段 12：动态 Logpoint

### T1201：提供可探测方法列表 API

- [ ] 返回当前构建中的方法 Probe ID 和源码位置。
- 验收：列表与编译清单一致。
- 依赖：T0603、T0503。

### T1202：实现探针控制转发

- [ ] Server 将 UI 控制命令转发给 Runtime。
- 验收：Runtime 收到指定 Probe ID 的开关状态。
- 依赖：T0108、T0203。

### T1203：实现 Runtime 探针配置表

- [ ] Runtime 根据配置决定是否生成快照。
- 验收：未启用探针只有轻量命中或完全跳过快照。
- 依赖：T1202、T0611。

### T1204：实现 UI 方法级 Logpoint

- [ ] 用户可在方法源码位置开启 Logpoint。
- 验收：开启后不重新编译即可看到参数和状态。
- 依赖：T1201、T1203。

### T1205：定义条件探针解释器

- [ ] 实现一个结构化条件操作符。
- 验收：先只支持 `eq`，不执行任意字符串代码。
- 依赖：T0108。

### T1206：增加数值比较条件

- [ ] 增加 `gt`、`gte`、`lt`、`lte`。
- 验收：每个操作符都有边界测试。
- 依赖：T1205。

### T1207：增加逻辑组合条件

- [ ] 增加 `all` 和 `any`。
- 验收：嵌套条件有深度限制并通过测试。
- 依赖：T1206。

### T1208：在 UI 中配置条件 Logpoint

- [ ] 提供结构化条件编辑界面。
- 验收：用户不需要输入可执行 JavaScript。
- 依赖：T1207、T1204。

## 15. 阶段 13：网络与导出

### T1301：调研微信网络请求统一接入点

- [ ] 明确 `wx.request` 和 Mpx 请求封装的接入策略。
- 交付物：网络接入研究记录。

### T1302：采集请求开始事件

- [ ] 上报 URL、方法和开始时间。
- 验收：示例请求产生一条开始事件。
- 依赖：T1301。

### T1303：采集请求完成事件

- [ ] 上报状态、耗时和响应摘要。
- 验收：完成事件能与开始事件关联。
- 依赖：T1302。

### T1304：实现网络时间线过滤

- [ ] 时间线支持只查看网络事件。
- 验收：过滤结果包含请求开始和完成。
- 依赖：T1303、T1103。

### T1305：定义 `.mpx-trace` 清单

- [ ] 定义 Trace 文件版本和内容索引。
- 验收：Schema 能描述事件、快照、Source Map 和语义映射。
- 依赖：T0101。

### T1306：导出 Trace

- [ ] 将当前调试会话导出为单个文件。
- 验收：导出文件可以解包并通过清单校验。
- 依赖：T1305、T0208。

### T1307：导入 Trace

- [ ] Server 能以只读方式加载一个 Trace。
- 验收：无运行设备时仍能查看历史事件。
- 依赖：T1306。

## 16. 阶段 14：生产隔离与性能

### T1401：验证生产构建不注入 Runtime

- [ ] 增加自动化测试检查生产产物。
- 验收：生产产物中不存在 `__MPX_DEBUG__` 和调试连接地址。
- 依赖：T0606。

### T1402：测量首次编译额外耗时

- [ ] 对比开启和关闭插件的首次编译时间。
- 验收：结果记录到性能报告。
- 依赖：T0605、T0006。

### T1403：测量增量编译额外耗时

- [ ] 对比开启和关闭插件的增量编译时间。
- 验收：结果记录到性能报告。
- 依赖：T1402。

### T1404：测量未命中探针开销

- [ ] 测量高频调用中关闭探针的额外耗时。
- 验收：测试方法、次数和结果可重复。
- 依赖：T1203。

### T1405：优化超出预算的单一指标

- [ ] 选择最先超出预算的一个指标进行优化。
- 验收：该指标回到预算内或记录不能达到的原因。
- 依赖：T1402、T1403、T1404。

## 17. 阶段 15：VS Code

### T1501：创建 VS Code 扩展空壳

- [ ] 扩展可以被本地安装和激活。
- 验收：命令面板出现一个 Mpx Debug 命令。

### T1502：从 VS Code 启动 Debug Server

- [ ] 命令可以启动和停止本地服务。
- 验收：状态栏正确显示 Server 状态。
- 依赖：T0201、T1501。

### T1503：嵌入调试 UI Webview

- [ ] 在 VS Code 中打开时间线界面。
- 验收：Webview 能显示 Server 推送的事件。
- 依赖：T0210、T1501。

### T1504：点击事件跳转源码

- [ ] Webview 消息触发编辑器打开源码。
- 验收：跳转到正确文件、行和列。
- 依赖：T0905、T1503。

### T1505：显示可探测方法位置

- [ ] 在编辑器中装饰可设置 Logpoint 的方法。
- 验收：装饰位置与 Probe ID 清单一致。
- 依赖：T1201、T1501。

### T1506：从编辑器切换 Logpoint

- [ ] 点击命令开启或关闭当前方法探针。
- 验收：不修改源码且不重新编译。
- 依赖：T1204、T1505。

## 18. 阶段 16：React Native

### T1601：建立 RN 最小示例项目

- [ ] 同一 Mpx 页面可以生成并运行 RN 目标。
- 验收：iOS 或 Android 至少一个目标运行成功。

### T1602：获取 Metro Source Map

- [ ] 找到当前 Bundle 对应的 Source Map。
- 验收：map 与 `buildId` 建立关联。
- 依赖：T1601、T0502。

### T1603：还原一个 RN 错误栈

- [ ] 将一个 Metro/Hermes Stack Frame 映射到 `.mpx`。
- 验收：固定错误准确定位源码。
- 依赖：T1602、T0506。

### T1604：实现 RN WebSocket Transport

- [ ] RN Runtime 可以连接现有 Debug Server。
- 验收：Server 识别 target 为 iOS 或 Android。
- 依赖：T0301、T0204。

### T1605：上报 RN 页面事件

- [ ] RN 适配器上报统一页面事件。
- 验收：UI 页面栈无需平台专用代码即可展示。
- 依赖：T1604、T0104。

### T1606：上报 RN 方法探针

- [ ] RN 构建中的方法探针命中后上报。
- 验收：参数和源码定位复用微信事件详情 UI。
- 依赖：T1603、T1604、T0607。

### T1607：实现平台时间线切换

- [ ] UI 可以在微信和 RN 会话之间切换。
- 验收：两个平台事件互不混淆。
- 依赖：T1605、T1606。

### T1608：实现双端事件对比

- [ ] 对同一语义 ID 的事件进行微信/RN 对照。
- 验收：至少比较生命周期顺序和方法耗时。
- 依赖：T1607。

## 19. 首个 POC 建议范围

如果准备开始开发，只执行以下最短链路，仍然每次只完成一个任务：

```text
T0001 → T0002 → T0003 → T0005
→ T0101 → T0102 → T0103 → T0106
→ T0201 → T0203 → T0204 → T0207
→ T0301 → T0302
→ T0501 → T0502 → T0504 → T0505 → T0506
→ T0601 → T0602 → T0603 → T0604 → T0605 → T0606 → T0607
```

POC 最终只证明一件事：

```text
点击微信小程序按钮
→ Mpx 方法探针命中
→ Server 收到事件
→ 生成代码位置还原到 .mpx 源码
```

完成这条链路后，再按照清单继续页面栈、参数、组件树和 UI。不要在 POC 阶段同时开发完整界面。

## 20. 当前进度

```text
当前任务：等待 T0210 验收
最后完成：T0210 将新事件推送给 UI
下一候选：T0301 建立 Runtime 核心接口
```
